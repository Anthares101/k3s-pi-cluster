- name: Taint master node
  become: true
  command: "kubectl taint nodes {{ master_node_name }} CriticalAddonsOnly:NoSchedule --overwrite"

- name: Add NFS provider
  become: true
  template:
    src: nfs-storage.yaml.j2
    dest: /var/lib/rancher/k3s/server/manifests/nfs-storage.yaml
